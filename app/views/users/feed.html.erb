<div class="row">
  <div class="col md-6 offset-3">
    <h1>
      <%= @user.username %>
    </h1>

    <ul class="nav nav-pills nav-justified">
      <li class="nav-item">
        <%= link_to "Posts", user_path(@user.username), class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Liked photos", liked_path(@user.username), class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Feed", feed_path(@user.username), class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Followers", followers_path(@user.username), class: "nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Following", following_path(@user.username), class: "nav-link" %>
      </li>
    </ul>
    <h2>Feed</h2>
  </div>
</div>

<ul>
  <%# feed_photos = [] %>
  <%# @user.leaders.each do |following_user| %>
    <%# following_user.own_photos.each do |photo| %>
      <%# feed_photos.push(photo) %>
    <%# end %>
  <%# end %>

  <% feed_photos = Photo.where(owner_id: @user.leaders).order(created_at: :asc) %>
  <% feed_photos.each do |photo| %>
    <%= render "photos/photo", photo: photo %>
  <% end %>
</ul>
